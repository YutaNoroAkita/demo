# Dev Task Tracker

タスク管理Webアプリです。HTML / CSS / Vanilla JS で実装されています。

## ファイル構成

| ファイル | 役割 |
|----------|------|
| `index.html` | HTMLテンプレート |
| `style.css`  | スタイル（ダークテーマ） |
| `app.js`     | アプリのロジック |

## 既知のバグ（3件）

1. **入力欄がクリアされない** — `addTask()` でタスク追加後、`input.value` がリセットされない
2. **完了カウンターが更新されない** — `toggleComplete()` 内で `updateStats()` が呼ばれていない
3. **削除が別のタスクに当たる** — `deleteTask()` 内の `findIndex` が `id + 1` を検索するオフバイワンエラー

## 未実装の機能

- **フィルター** — 「全て / アクティブ / 完了済み」でタスクを絞り込む
- **テキスト検索** — キーワードでタスクを検索する

## 技術仕様

- フレームワーク: なし（Vanilla JS）
- スタイル: CSS カスタムプロパティ（ダークテーマ）
- データ: インメモリ（`tasks` 配列）、永続化なし

## コード規約

- 関数名はキャメルケース
- DOM 操作は `getElementById` / `innerHTML` を使用
- XSS 対策として文字列は必ず `escapeHtml()` を通す
